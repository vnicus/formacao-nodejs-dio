//pode ser exportado variaves const, não fica limitado apenas na exportação de funçao
const devArea = {

    version: "01ab",
    production: true,

};

const client = {

    device: "web"
    
}


module.exports = {
    devArea,
    client
};